import java.util.Scanner;
//Crea un programa en Java que use operadores de asignación compuesta (+=, -=, *=, /=) con una variable entera y muestre los resultados.
public class ejercicio4 {
    public static void main(String[] args) {
        int numero = 4;
        int suma, resta, multiplicacion;
        double division;

        numero += 1;
    }
}
